# AEGIS Writing - Docker Compose
# 실행: docker-compose up -d

version: '3.8'

services:
  aegis-writing:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
